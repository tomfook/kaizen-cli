# Project Learnings

プロジェクト固有の教訓・制約・落とし穴を記録する。
汎用パターンに昇格したら `knowledge/` に移動し、ここからは削除する。

---

## CI

- **CI 導入時は初回 green を確認する**: Link Check CI を追加した際、初回パスを確認せずマージした結果、全ビルドで失敗し続けていた。新しい CI ワークフローを追加したら、マージ前に passing を確認すること。

## PR レビュー

- **機能重複PRでも実装品質を比較する**: PR #31 と dev ブランチで同じ git init 機能が実装されていたが、クローズせず比較したところ、PR #31 の方がエッジケース対応（git 未インストール、user 未設定、GPG 署名）に優れていた。「重複 → クローズ」ではなく「品質の高い方を採用」が正しいアプローチ。
